[package]
name = "libagent"
version = "0.1.0"
edition = "2021"

[dependencies]
omniagent-syscall = { path = "../omniagent-syscall" }
omniagent-ipc = { path = "../omniagent-ipc" }
serde = { version = "1.0", default-features = false, features = ["derive", "std"] }
